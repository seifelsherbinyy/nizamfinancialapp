# Requirements — Foundation & Scaffolding

> KIRO spec for Contract 1. Full contract: `contracts/CONTRACT_1_*.md`. Read steering first.

## User story
As the sole user, I want foundation & scaffolding so the NIZAM build advances one verifiable contract.

## Acceptance criteria (EARS)
- THE SYSTEM SHALL initialize a Vite+React+TS project that builds with `npm run build`.
- THE SYSTEM SHALL provide an integer-milliunit money module with passing unit tests.
- THE SYSTEM SHALL define core domain types (Account, Category, Transaction, MonthBudget).
- WHEN `npm run typecheck` runs THE SYSTEM SHALL report zero errors.
