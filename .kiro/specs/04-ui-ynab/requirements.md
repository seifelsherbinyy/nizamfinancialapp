# Requirements — UI / UX (YNAB-style)

> KIRO spec for Contract 4. Full contract: `contracts/CONTRACT_4_*.md`. Read steering first.

## User story
As the sole user, I want ui / ux (ynab-style) so the NIZAM build advances one verifiable contract.

## Acceptance criteria (EARS)
- THE SYSTEM SHALL render a budget grid with editable Assigned and RAG Available.
- THE SYSTEM SHALL render an accounts sidebar with balances.
- THE SYSTEM SHALL render a transaction register with inline edit + cleared toggle.
- THE SYSTEM SHALL provide add/edit transaction with splits and transfers.
- THE SYSTEM SHALL provide a reconciliation flow and an import wizard.
