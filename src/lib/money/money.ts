/**
 * NIZAM · Integer money core — milliunits, no floats (EGP=1000)
 * Implemented by: KIRO Contract 1 / Phase 4
 * Status: PLACEHOLDER — build per .kiro/specs + contracts/CONTRACT_1_*.md
 * Depends on: none
 * TODO:
 *   - type Money = number (integer milliunits)
 *   - fromDecimal/toDecimal
 *   - add/sub/mul/allocate (banker split)
 *   - format(currency, locale ar-EG/en)
 */
export {}; // placeholder — replace on implementation
