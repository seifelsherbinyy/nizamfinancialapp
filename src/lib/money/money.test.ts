// NIZAM · money core tests (Contract 1 / Phase 5) — PLACEHOLDER
// TODO: no float drift, allocate() sums exactly, round-trip decimal<->milliunits, negative handling.
import { describe, it, expect } from 'vitest';
describe.skip('money (implement in Contract 1)', () => { it('placeholder', () => { expect(true).toBe(true); }); });
